<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Midnight Poems</title>

  <!-- Fonts: Cinzel Decorative (título), Cormorant Garamond (poético), Inter (UI) -->
  <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700;900&family=Cormorant+Garamond:wght@300;400;600&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <!-- Nebula animated layers -->
  <div class="nebula nebula--layer1" aria-hidden="true"></div>
  <div class="nebula nebula--layer2" aria-hidden="true"></div>

  <!-- Starfield canvas (script stars.js) -->
  <canvas id="starfield" aria-hidden="true"></canvas>

  <header class="hero" role="banner">
    <div class="hero-inner">
      <h1 class="title">MIDNIGHT POEMS</h1>
      <p class="subtitle">Um site sobre os poemas mais bonitos que existem: as estrelas.</p>
    </div>
  </header>

  <main id="grid" class="grid container" role="main" aria-label="Lista de constelações">
    <% lista.forEach(c => { %>
      <article class="card"
               tabindex="0"
               data-id="<%= c._id %>"
               data-nome="<%= c.nome %>"
               data-imagem="<%= c.imagem %>"
               data-forma="<%= c.formaImg %>"
               data-historia="<%= c.historia %>"
               data-epoca="<%= c.epocaVisivel %>">

        <div class="card-media" aria-hidden="true">
          <img class="layer-bg" src="<%= c.background %>" alt="">
          <img class="layer-stars" src="<%= c.imagem %>" alt="">
          <div class="glow" aria-hidden="true"></div>
        </div>

        <div class="card-content">
          <h2 class="card-title"><%= c.nome %></h2>
          <p class="card-sub"><%= c.comentario %></p>
        </div>
      </article>
    <% }) %>
  </main>

  <div id="overlay" class="overlay" aria-hidden="true"></div>

  <!-- scripts (mantive stars.js e main.js que você já tem) -->
  <script src="/js/stars.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>
