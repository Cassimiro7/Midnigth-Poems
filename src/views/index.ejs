<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Midnight Poems</title>

    <link rel="stylesheet" href="/css/style.css">
</head>

<body>

    <!-- Fundo animado (versão vídeo ou partículas) -->
    <div class="bg-video">
        <video autoplay loop muted playsinline>
            <source src="/videos/night-sky.mp4" type="video/mp4">
        </video>
    </div>

    <!-- Título principal -->
    <header class="hero">
        <h1>MIDNIGHT POEMS</h1>
        <p>poemas guiados pelas estrelas</p>
    </header>

    <!-- Grid de constelações -->
    <main class="grid">

        <% lista.forEach(c => { %>

            <div class="card">
                <img src="<%= c.imagem %>" alt="<%= c.nome %>">
                <h2><%= c.nome %></h2>
            </div>

        <% }) %>

    </main>

</body>

</html>
